"use server"

import { CompanyInformation } from "../models/CompanyInformation"

function GetCompanyInformation(identificator: number) : CompanyInformation | undefined {
    // TODO: Fetch the api data, use GET, with the identificator

    return undefined;
}
